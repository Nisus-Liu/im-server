



参考野火IM项目, https://docs.wildfirechat.cn/
前端 vue, 后端Java+netty,
基于mqtt协议通信.
补充通信部分实现.
后端是moqutte代码复制过来修改的, 但是缺少了通信部分核心代码
中文回答, 客户端发送消息是野火IM的web端js代码发送的, 大概是结合 mqtt+websocket的方式. 消息体格式很定是对的.

ws 第三个data
One or more reserved bits are on: reserved1 = 0, reserved2 = 1, reserved3 = 0

客户端代码是编译压缩后的js代码, 经过尝试改造, 还是不能正常通信.

现在, 根据已有的代码基础. 
自己实现客户端和服务端通信部分.
